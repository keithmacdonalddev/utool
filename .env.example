# =============================================================================
# PROJECT-WIDE CONFIGURATION
# =============================================================================
# Node environment: development, production, test
# Controls behavior of both client and server applications
NODE_ENV=development

# Application name used across the entire project
APP_NAME=uTool

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
# Base URL where the application is deployed
# Used in various places including absolute URL generation
BASE_URL=http://localhost:3000

# Vercel-specific configuration for deployment
# Only needed when deploying to Vercel
VERCEL_PROJECT_ID=your_vercel_project_id
VERCEL_ORG_ID=your_vercel_org_id
